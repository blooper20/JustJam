# JustJam Project 작업판 (Task Board)

## 🎯 V1.0 런칭 필수 과제

### [🟩 완료됨 (Done)]
- [x] **도커 볼륨 마운트 경로 불일치로 인한 오디오 트랙 404 로드 실패 버그 수정**
  - 클라이언트 측 fetchProjectStems 에서 NEXT_PUBLIC_API_URL을 상대 경로에 prepend하도록 수정.
- [x] **WaveSurfer.js 다중 트랙 렌더링 시 발생하는 프론트엔드 버벅거림 최적화**
  - 순차적 지연 초기화(Staggered Init), `partialRender` 도입, 하이드레이션 방어(`ssr: false`) 적용.
- [x] **음원 분리(Demucs) 로드 버그 및 도커 볼륨 동기화**
  - 45초 청킹(Chunking) 처리 및 Celery `concurrency=1` 적용으로 OOM 방어.
- [x] **프론트/백엔드 잔여 기술 부채 청산**
  - TanStack Query 1초 폴링 최적화(단락 제어 및 3회 연속 실패 중지), flake8 린트 강제 교정 완료.
- [x] **악보/타브(Score/Tab) 잔재 완전 제거 및 문서 현행화**
  - 프론트/백엔드 데드코드 완전 소거 및 README.md / README_KR.md 다국어 스펙 통합 완료.

---

## 🚀 V1.1 포스트 런칭: 밴드 협업 시스템 (Epics)

### [🟩 완료됨 (Done)]
- [x] **멤버 초대 및 워크스페이스:** DB 기반 팀원 초대 및 프로젝트 권한(Viewer/Editor) 관리.
- [x] **밴드 소통 보드 (공지 / 투표):** 일반 공지 + 투표 생성(유튜브 임베드 포함) + 댓글 기능.
  - 포스트 업로드 버그 수정 (`post_options.youtube_url` DB 컬럼 누락 → 직접 ALTER TABLE)
  - 빈 필드 제출 시 에러 토스트 표시 (기존: 조용히 실패)
- [x] **일정 조율 탭 독립 분리:**
  - 우측 인덱스 탭에 SCHEDULE 탭(보라색) 추가 → NOTICE / SCHEDULE / SONG / PRACTICE 4탭 구조
  - `ScheduleBoard` 컴포넌트 신규 생성 — 제목 + 캘린더 날짜 선택으로만 등록, 본문 불필요
  - `CollaborationBoard`에서 일정 코드 완전 분리 — 공지/투표 전담
- [x] **연습 인증 캘린더 및 숏폼 셋로그 (Setlog) 시스템:** 합주 멤버들의 참여도를 높이고 연습 기록을 남기기 위한 숏폼 기반 인증 시스템을 구축한다.
  - **동영상 촬영 (15초 제한):** 웹캠/모바일 카메라를 연동하여 플랫폼 내에서 직접 동영상을 촬영할 수 있으며, 촬영 길이는 최대 15초로 제한한다.
  - **5초 구간 크롭 편집기 (Fixed 5-sec Trimming):**
    - 촬영(또는 업로드) 완료 후 즉시 편집 창으로 이동한다.
    - 타임라인 슬라이더의 길이는 **'무조건 5초'로 고정**된다.
    - 사용자는 15초의 원본 영상 위에서 이 5초짜리 슬라이더를 좌우로 움직여 원하는 시작/끝 구간을 자유롭게 선택하여 저장할 수 있다.
  - **텍스트 오버레이 (Text Overlay):**
    - 편집된 5초 영상의 정중앙에 사용자 지정 텍스트를 삽입하는 기능을 제공한다.
    - 시인성을 위해 텍스트는 **최대 20글자로 엄격히 제한**한다.
    - 저장 시 이 텍스트가 영상에 하드코딩(Burn-in) 되거나 프론트엔드 UI 컴포넌트로 영상 위 중앙에 항상 렌더링되도록 처리한다.
- [x] **오토 브이로그(Auto-Vlog):** 연습 영상들을 하나의 셋로그 브이로그로 자동 병합.
- [x] **연습 인증 영상 일일 업로드 제한 정책 변경 및 댓글 기능 구현:**
  - **개인별 일일 업로드 제한:** 오늘 날짜 또는 특정 날짜에 다른 유저가 영상을 올렸더라도 본인(로그인 유저)이 올리지 않았다면 업로드가 가능하며, 로그인 유저 본인이 이미 해당 날짜에 영상을 올린 경우에만 업로드 UI를 숨기고 완료 메시지를 노출하도록 제어.
  - **연습 코멘트 입력 칸 제거:** 업로드 폼에서 기존의 한 줄 소감(코멘트) 입력란을 삭제.
  - **연습 로그 댓글 시스템 도입:** 업로드된 각각의 연습 영상(연습 로그) 하단에 팀원들이 감상평 및 피드백 댓글을 달고 삭제할 수 있는 댓글 영역을 구현.
- [x] **송보드 (SongBoard) 대시보드 인라인 통합:** `/projects` 페이지를 밴드 협업 대시보드의 `SONG` 탭으로 병합하여, 오른쪽 인덱스 스티커 탭을 클릭할 때 전체 페이지 리프레시나 라우팅 전환 없이 컴포넌트 단위로 부드럽게 화면이 전환되도록 구현. `/projects` URL 및 상세 페이지의 복귀 주소도 `?tab=song`으로 리다이렉션 연동 완료.
- [x] **팀 멤버 수 계산 시 본인(소유자) 포함 처리:** 백엔드 자가 치유(Self-Healing) 모델 데이터 자동 보정과 프론트엔드 Fallback 카운팅 처리를 통해 본인을 멤버 수에 정상 포함하도록 개선.

---

## 🔧 현재 알려진 버그 / 미완 기능

### [🟥 버그]
- (남은 버그 없음)

### [🟨 미완 / 개선 필요]
- (남은 미결 과제 없음)

### [🟩 완료된 버그 수정 및 개선 사항 (Completed)]
- [x] **`dd2592a4fe85` 팀 모델 마이그레이션 미반영 의심 해결**
  - `abd4c78bed95` (youtube_url) 모델과 마이그레이션 동기화 완료.
- [x] **셋로그(Setlog) 구간 편집기를 아이폰 기본 카메라 동영상 편집(Trimmer) 스타일로 전환**
  - 썸네일 스트립(Filmstrip) 노란색 조절 프레임 박스 UI 및 실시간 재생 헤드(Playhead) 연동 완료.
- [x] **일정 조율 — 확정 날짜 선택 및 확정 기능 구현**
  - 관리자 최종 합주 일정 날짜 "확정" 및 저장 기능 구현 완료.
- [x] **연습 캘린더(PracticeCalendar) 비디오 업로드 오류 처리 완료**
  - `create_practice_log` 및 `search_users` 라우트의 `team_id` 참조 문제 교정 완료.
- [x] **연습 인증 캘린더 (셋로그) 비디오 구간 탐색, 텍스트 오버레이, 재편집 고도화**
  - 5초 윈도우 클릭 센터 정렬, 원본 파일(`raw_video_url`) 기반 PUT 재편집 기능 및 스키마 자가 치유 마이그레이션 적용 완료.
- [x] **댓글 실시간 동기화**
  - 1초 주기 단락 제어 기반 실시간 댓글 동기화 폴링 처리 완료.
- [x] **모바일 반응형**
  - 우측 스티커 인덱스 탭 모바일 반응형 터치 레이아웃 및 3컬럼 가변 그리드 적용 완료.
- [x] **FastAPI 422 에러 메시지**
  - 프론트엔드 에러 발생 시 422 디테일 객체 파싱 토스트 가독성 개선 완료.
- [x] **댓글 작성 중복 전송 버그 해결**
  - 댓글 입력 영역을 form 태그 기반으로 리팩토링하고, mutation이 pending 상태일 때 입력 필드와 전송 버튼을 비활성화 및 얼리 리턴 처리하여 중복 제출(double-submit) 문제를 원천 차단.
- [x] **밴드 멤버 초대 검색 자동완성 구현**
  - 밴드 멤버 초대 시 이메일을 일일이 직접 입력하는 수고를 덜기 위해, 백엔드에 팀 전용 가입자 검색 API (`/teams/{team_id}/search-users`)를 추가하고 프론트엔드에 실시간 검색(Auto-Suggest) 드롭다운 UI를 탑재하여 클릭 한 번으로 손쉽게 초대 가능하도록 개선. 초대 실패 시 백엔드에서 리턴한 상세 사유가 토스트로 노출되도록 보완.
- [x] **설정 페이지 UI/UX 개선 및 로고 엑스박스 버그 해결**
  - **대시보드식 리팩토링**: 기존 1열로만 렌더링되던 단조로운 레이아웃을 아름다운 다크 테마 감성의 2컬럼 그리드 대시보드 구조로 전면 리뉴얼.
  - **기능성 컴포넌트 추가**: 프로필 변경 외에 언어 번역 동기화, 화면 테마 설정, 세부 알림 토글(공지/투표/댓글 3개 채널 분리) 가상 스위치 UI 제공 및 `localStorage` 영구 보존 연동.
  - **헤더 로고 엑스박스 해결**: Next.js Auth Middleware(`proxy.ts`) matcher 설정에 `/images` 경로가 누락되어 로고 이미지가 로그인 페이지로 리다이렉트(307)되며 깨져 보이던 고질적인 보안 게이트 버그 수정.

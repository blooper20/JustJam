# 📋 JustJam 통합 작업판 (Task Board)

본 작업판은 JustJam 프로젝트의 결함 상태(To-Fix), 향후 고도화 및 미완성 기능(To-Do), 작업 진행도를 추적하는 칸반 보드(Kanban), 그리고 재발 방지를 위한 영구 에러 래칫 규칙(Ratchet Rules)을 통합 관리하는 문서입니다.

---

## 🚨 현재 발생 중인 결함 및 경고 (Active To-Fix)

1. **Pydantic V2 마이그레이션 경고 (`PydanticDeprecatedSince20`)**
   - **현상**: `Field(..., example=...)` 등 Deprecated 인자 사용.
   - **해결책**: `Field(..., json_schema_extra={"example": ...})`로 전환.

2. **FastAPI 기동 이벤트 핸들러 마이그레이션 (`on_event`)**
   - **현상**: `@app.on_event("startup")` 데코레이터 사용 중단 권장.
   - **해결책**: `lifespan` 컨텍스트 매니저 사용하도록 교체.

3. **`middleware.ts` 및 `route.ts` strict typing 적용**
   - **현상**: `as any` 임시 캐스팅 존재.
   - **해결책**: `next-auth.d.ts` 모듈 확장 등으로 엄격 타입 정의.

4. **Mypy 컴파일러 Namespace 충돌 및 정적 타입 오류**
   - **현상**: `auth` 중복 모듈 충돌 및 SQLAlchemy/Pydantic 타입 불일치.
   - **해결책**: `pyproject.toml` 내 `explicit_package_bases = true` 설정 및 타입 교정.

5. **Next.js 16 (Turbopack) Middleware Convention Deprecated 경고**
   - **현상**: `middleware.ts` 파일 대신 `proxy` 사용 경고.
   - **해결책**: 공식 문서에 따라 프록시(`proxy`) 설정 전환.

6. **Pydantic V2 ORM 객체 마이그레이션 누락 경고 (`from_orm`)**
   - **현상**: `.from_orm()` 호출로 경고 발생.
   - **해결책**: `from_attributes = True` 지정 및 `model_validate()` 일괄 전환.

### 🟩 해결 완료된 문제
- **API 스키마 불일치 (Assertion Error)**: `src/api/routes/projects.py`와 `test_workflow.py` 간 반환타입/스키마 정합성 일치화 완료.
- **파이썬 린트 (`flake8`) 에러**: 미사용 임포트, 라인 초과, 비교문 표기 등 일괄 교정 완료.
- **프론트엔드 ESLint/Prettier 에러**: 63건의 린트 에러 전면 제거.

---

## 🚧 기능 고도화 및 확장 로드맵 (Roadmap)

### 1. 완료된 작업 요약
- **SSR 하이드레이션 에러 차단**: WaveSurfer 컴포넌트 dynamic 래핑 및 `isMounted` 클라이언트 마운트 제어 (Rule #402 준수).
- **상태 폴링 최적화 (`use-project.ts`)**: TanStack Query의 `refetchInterval` 동적 콜백화, 완료/실패 시 및 3회 이상 실패 시 즉시 단락.
- **Celery Worker 동시성 제어**: GPU OOM 방지를 위해 `worker_concurrency=1` 단일 큐 제한 반영.
- **Basic Pitch 퀀타이즈 및 악보 보완**: 16분음표/셋잇단음표 스냅 그리드 스위칭, 도약 페널티 및 `chord.Chord` 자동 병합 적용.
- **타브 악보 운지 최적화 및 퀀타이징 고도화**: 인접 줄 이동성(String Skip)과 4프렛 도약 페널티 강화, 음표 길이 퀀타이즈 추가.

### 2. 향후 고도화 계획
- **대기열 실시간 SSE 알림**: Celery Task 대기열 상태 및 대기 시간 실시간 푸시.
- **WaveSurfer.js 재생부 동적 청킹**: 대용량 오디오 디코딩 오버헤드 최소화.
- **다중 AI 모델 지원**: RoFormer, MDX-Net 등 분석 시 모델 선택 옵션 확장.
- **타브 악보 운지법 최적화 및 익스포트**: DP 기반 최적화 및 GuitarPro (`.gp5`) 파일 내보내기 모듈 개발.

---

## 📋 작업 추적 칸반 보드 (Kanban)

### ⬜ 진행 전 (To-Do List)
- [ ] Backend: mypy `--explicit-package-bases` 활성화 및 정적 타입 오류 전면 해소
- [ ] Frontend: Next.js 16 (Turbopack) `middleware` 컨벤션을 신규 `proxy` 사양으로 마이그레이션
- [ ] Backend: Pydantic V2 `.from_orm()` 호출을 `.model_validate()` 및 `from_attributes=True` 사양으로 마이그레이션
- [ ] Backend/Frontend: 대기열 실시간 SSE(Server-Sent Events) 알림 채널 구축
- [ ] Frontend: WaveSurfer.js 재생부 동적 청킹 및 디코딩 레이턴시 최소화 로직 도입
- [ ] Pipeline: 다중 AI 음원 처리 모델 선택 옵션 구현
- [ ] Algorithm: 타브 악보 운지법 최적화 적용 및 GuitarPro (.gp5) 내보내기 모듈 개발

### 🟦 진행 중 (In Progress)
*(현재 진행 중인 작업 없음)*

### 🟩 완료됨 (Done)
- [x] Frontend: `multitrack-player.tsx` SSR 방어막 도입 (Rule #402 준수)
- [x] Frontend: Zustand 및 TanStack Query 폴링 최적화 (`refetchInterval` 단락 제어)
- [x] Frontend: ESLint/Prettier 린트 에러 63건 완전 소거 (Clean Pass)
- [x] Frontend: Next.js production 빌드 컴파일 무결 확인
- [x] Backend: FastAPI `flake8` 위반 사항 및 미사용 임포트 일괄 제거
- [x] Backend: Celery 단일 컨커런시 큐(`worker_concurrency=1`) 구성 완료
- [x] Algorithm: 하이브리드 퀀타이징 및 역할별 Adaptive Noise Gate 구현 완료
- [x] Algorithm: 프렛 도약 페널티 및 동시성 현 중복 충돌 차단 적용 완료
- [x] Algorithm: 온셋 음표 `music21` Chord 자동 병합 및 가독성 최적화 완료
- [x] DevOps: API 라우터와 테스트 간의 정합성 검증용 Mock 테스트 구현 완료 (Rule #405 준수)
- [x] DevOps: GitHub Actions Python 캐시 경로 누락 및 `shell: bash` 설정 보완
- [x] DevOps: GitHub Actions Node.js 24 환경변수 주입
- [x] Documentation: 다국어 스펙 문서 1:1 동기화 완료 (`README.md`, `README_KR.md`, `SUMMARY.md`, `AGENTS.md`)
- [x] Verification: 백엔드 `pytest` 테스트 51개 전원 통과 완료

---

## 🛡️ 영구 진화형 에러 래칫 규칙 (Ratchet Rules)

- **Rule #402: Web Audio API / WaveSurfer 브라우저 샌드박스 원칙**
  - 브라우저 전용 API/객체는 Next.js SSR 및 빌드 타임에 실행되면 안 됨. `{ ssr: false }` dynamic 임포트 필수 및 `useEffect`/`window !== 'undefined'` 분기 사용.
- **Rule #403 & #404: CI/CD 파이프라인 캐시 및 의존성 방어 법칙**
  - 캐시 경로 널(Null) 방지 및 OS별 쉘 편차 방지를 위해 `shell: bash` 명시. Node.js 24 강제 설정(`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`).
- **Rule #405: API-테스트 코드 스키마 무결성 동기화 의무**
  - 백엔드 API 스키마 변경 시 테스트 검증 로직도 함께 변경하여 CI 붕괴 방지. 린트 에러(`flake8` Exit Code 1) 존재 시 푸시 차단.
- **Rule #406: Next.js 빌드 타임 조건부 컴파일러 Directive 예방**
  - 빌드 시 Unused 경고로 컴파일이 막히지 않도록 `@ts-expect-error` 단독 사용 대신 `as any` 캐스팅과 ESLint 비활성화를 적절히 조합.
- **Rule #407: Mypy Explicit Package Bases 및 정적 타입 무결성**
  - `explicit_package_bases = true` 설정으로 네임스페이스 충돌 방지 및 standard type annotation 준수.
- **Rule #408: Next.js 16 Turbopack 하위 호환성 확보 및 Proxy 교체**
  - `npm run build` 시 deprecation warning을 유발하는 구형 middleware 구성을 proxy 형식으로 마이그레이션 의무화.

---

## 🤖 JustJam AI Sub-Agents

- **Agent Alpha (프론트엔드/DSP)**: Next.js 16, React 19, Zustand, WaveSurfer.js 등 클라이언트 상태 및 재생기 제어 담당.
- **Agent Beta (백엔드/AI)**: FastAPI, Pydantic, SQLAlchemy, Celery/Redis 기반 AI 모델 분리/전사 파이프라인 담당.
- **Agent Gamma (DevOps/QA)**: Lint(`flake8`, `black`, `eslint`) 강제, 테스트 정합성 감시, CI/CD 캐시 및 패키징 수호 담당.

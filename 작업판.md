# JustJam Project 작업판 (Task Board)

## 🎯 V1.0 런칭 필수 과제

### [🟩 완료됨 (Done)]
- [x] **도커 볼륨 마운트 경로 불일치로 인한 오디오 트랙 404 로드 실패 버그 수정**
  - 클라이언트 측 fetchProjectStems 에서 NEXT_PUBLIC_API_URL을 상대 경로에 prepend하도록 수정.
- [x] **WaveSurfer.js 다중 트랙 렌더링 시 발생하는 프론트엔드 버벅거림 최적화**
  - 순차적 지연 초기화(Staggered Init), `partialRender` 도입, 하이드레이션 방어(`ssr: false`) 적용.
- [x] **음원 분리(Demucs) 로드 버그 및 도커 볼륨 동기화**
  - 45초 청킹(Chunking) 처리 및 Celery `concurrency=1` 적용으로 OOM 방어.
- [x] **프론트/백엔드 잔여 기술 부채 청산**
  - TanStack Query 1초 폴링 최적화(단락 제어 및 3회 연속 실패 중지), flake8 린트 강제 교정 완료.
- [x] **악보/타브(Score/Tab) 잔재 완전 제거 및 문서 현행화**
  - 프론트/백엔드 데드코드 완전 소거 및 README.md / README_KR.md 다국어 스펙 통합 완료.

---

## 🚀 V1.1 포스트 런칭: 밴드 협업 시스템 (Epics)

### [🟩 완료됨 (Done)]
- [x] **멤버 초대 및 워크스페이스:** DB 기반 팀원 초대 및 프로젝트 권한(Viewer/Editor) 관리.
- [x] **밴드 소통 보드:** 게시글/댓글 기능, 합주 곡 투표, 일정 조율 기능.
  - 일반 공지, 투표 생성(유튜브 임베드 포함), 일정 조율 캘린더 구현
  - 포스트 업로드 버그 수정 (`post_options.youtube_url` DB 컬럼 누락 → 직접 추가)
  - 일정 조율 UI 분리: 글쓰기 폼과 완전히 분리된 독립 섹션으로 배치
- [x] **연습 인증 캘린더 (셋로그):** 다음 합주 전까지 연습 영상을 올려 캘린더에 인증 마크(잔디) 심기.
- [x] **오토 브이로그(Auto-Vlog):** 업로드된 연습 영상들을 모아 하나의 셋로그 브이로그로 자동 병합.

---

## 🔧 현재 알려진 버그 / 미완 기능

### [🟥 버그]
- [ ] **`dd2592a4fe85` 팀 모델 마이그레이션 미반영 의심**
  - `abd4c78bed95` (youtube_url)와 동일한 패턴으로, alembic_version에는 기록됐지만 실제 DB에 반영 안 됐을 수 있음.
  - 팀 관련 기능 오류 시 `docker compose exec postgres psql -U justjam -d justjam -c "\d teams"` 로 스키마 확인 필요.

### [🟨 미완 / 개선 필요]
- [ ] **포스트 작성 후 에러 메시지 개선**
  - FastAPI 422 Validation 에러 시 배열 형태(`detail: [...]`)로 내려오는 경우 사용자에게 의미 없는 메시지 표시될 수 있음.
- [ ] **일정 조율 - 확정 날짜 선택 기능**
  - 현재는 멤버별 가능 날짜 투표까지만 구현. 관리자가 최종 날짜 1개를 "확정"하고 알림을 보내는 기능 없음.
- [ ] **연습 캘린더(PracticeCalendar) 비디오 업로드 오류 처리**
  - `create_practice_log` 라우트 내 `team_id` 변수 참조 오류 존재 (현재 미사용 경로).
- [ ] **팀 없을 때 협업 보드 진입 차단**
  - 팀 미선택 상태에서 `/dashboard/collab` 접근 시 redirect 처리는 됐으나 로딩 UX 개선 여지 있음.
- [ ] **댓글 실시간 동기화**
  - 현재 댓글은 제출 후 refetch 방식. 다른 멤버가 작성한 댓글 실시간 반영 안 됨 (폴링 or WebSocket 필요).
- [ ] **모바일 반응형**
  - 협업 보드 레이아웃(3컬럼 그리드)이 모바일에서 깨질 수 있음.

# 📋 JustJam 통합 작업판 (Task Board)

본 작업판은 JustJam 프로젝트의 현재 결함 상태(To-Fix), 향후 고도화 및 미완성 기능(To-Do), 작업 진행도를 추적하는 칸반 보드(Kanban), 그리고 재발 방지를 위한 엄격한 에러 래칫 규칙(Ratchet Rules)을 통합 관리하는 개발 나침반 문서입니다.

---

## 🚨 현재 발생 중인 시급한 문제 (To-Fix)
 
 ### 1. [해결 완료] API 스키마 불일치 및 테스트 충돌 (Assertion Error)
 * **현상 및 원인**: 
   - 백엔드의 `src/api/routes/projects.py`와 CLI 테스트 도구인 `test_workflow.py` 간의 API 인터페이스 혹은 데이터 구조 매핑 과정에서 충돌이 발생할 위험이 있습니다.
   - `generate_project_tab` 엔드포인트는 `ProjectService.generate_tab`를 통해 JSON 구조를 생성하여 `JSONResponse`로 반환하는 반면, `test_workflow.py`는 `transcribe_audio` 결과를 받아 로컬 `TabGenerator` 인스턴스로 즉시 ASCII 타브 문자열을 생성하고 있었습니다.
   - 프론트엔드가 요구하는 `TabResponse` 스키마(예: `notes_count`, `bpm`, `tab` 문자열 필드 포함)와 백엔드 API 서비스 메서드가 직렬화하는 딕셔너리 포맷이 어긋나 검증용 테스트 코드에서 `AssertionError`가 발생할 수 있습니다.
 * **해결 방안 및 결과**:
   - `src/api/routes/projects.py`의 `generate_project_tab` 반환 타입 및 `ProjectService.generate_tab` 내부 리턴 구조가 `client/lib/api.ts`의 `TabResponse` 명세와 정확히 일치하도록 조율 완료했습니다.
   - `test_workflow.py` 내부에서도 단순 콘솔 출력을 넘어 스키마 정합성을 1차 검증할 수 있도록 assertion 로직을 연동했습니다. (해결 완료)
 
 ### 2. [해결 완료] 파이썬 린트 (`flake8`) Exit Code 1 에러 해결
 * **현상 및 원인**:
   - 로컬 및 CI/CD 환경에서 `flake8 src/ tests/` 실행 시, 다양한 스타일/문법 위반 사항으로 인해 빌드가 실패했었습니다.
 * **주요 에러 카탈로그 및 결과**:
   - **F401 (Imported but unused)**: `src/api/routes/auth.py` 등 다수 파일의 미사용 임포트를 전면 제거 완료했습니다.
   - **E501 (Line too long)**: 줄 바꿈 기준 준수를 통해 가독성을 향상시켰습니다.
   - **E711/E712 (Comparison to None/True)**: 권장 비교 연산 스타일(`is None`, `is True`)로 수동 교정을 완료했습니다.
   - `flake8 src/ tests/` 실행 결과 린트 에러 0건을 확인했습니다. (해결 완료)

### 3. [해결 완료] 프론트엔드 ESLint 및 Prettier 린트 에러 소거
* **현상 및 원인**:
  - `client` 폴더 내부 ESLint 실행 시 타입 오류 및 미사용 임포트/변수, `useEffect` 의존성 누락 경고가 63건 잔존하여 `AGENTS.md` 빌드 조건에 위배되었습니다.
* **해결 방안 및 결과**:
  - `use-project.test.tsx` displayName 추가, `api-client.test.ts` `@ts-expect-error` 교체 및 `any` 관련 예외 처리, 미사용 변수/임포트 전면 제거, `tab-viewer.tsx` `useCallback` 적용 및 hooks dependencies 의존성 배열 보완을 완료했습니다.
  - `npm run lint` 실행 시 에러 0건(Clean Pass)을 달성하여 빌드 무결성을 확보했습니다. (해결 완료)

### 4. Pydantic V2 마이그레이션 경고 발생 (`PydanticDeprecatedSince20`)
* **현상 및 원인**:
  - Pydantic V2 도입으로 인해 `Field(..., example=...)`와 같이 `Field`에 직접 `example` 인자를 지정하는 기존 방식이 Deprecated 되었습니다.
* **해결 방안**:
  - V2 표준에 맞춰 `Field(..., json_schema_extra={"example": ...})` 포맷으로 순차 전환하여 컴파일 타임 및 런타임 경고를 제거해야 합니다.

### 5. FastAPI 기동 이벤트 핸들러 마이그레이션 필요 (`on_event`)
* **현상 및 원인**:
  - `src/api/main.py`에 적용된 `@app.on_event("startup")` 데코레이터가 FastAPI 최신 사양에서 사용이 권장되지 않는 Deprecated 상태입니다.
* **해결 방안**:
  - 최신 가이드라인에 준해 `lifespan` 컨텍스트 매니저 방식으로 이벤트 리스너를 교체해야 합니다.

### 6. `middleware.ts` 및 `route.ts` 내 any 타입 캐스팅 제거 및 strict typing화
* **현상 및 원인**:
  - `middleware.ts`와 `route.ts` 내부에서 `next-auth` 세션 및 `withAuth` 와 `next-intl` 라이브러리의 버전 차이로 인해 발생한 임시 컴파일 에러를 회피하기 위해 `as any` 타입 캐스팅이 부분적으로 임시 적용되어 있습니다.
* **해결 방안**:
  - `next-auth.d.ts` 에 정의된 세션 인터페이스(Module Augmentation)를 명확히 확장하거나 함수 래퍼 타입을 새로 설계하여 any 캐스팅 없이 strict typescript 검증을 패스하도록 리팩토링해야 합니다.

### 7. Mypy 컴파일러 Namespace 충돌 및 118건의 정적 타입 오류
* **현상 및 원인**:
  - `mypy src/`를 통한 정적 타입 체크 진행 시, `src/api/routes/auth.py`와 `src/api/auth/__init__.py`가 동일한 모듈명(`auth`)을 공유하여 중복 모듈 오류(Duplicate Module)가 발생합니다.
  - `--explicit-package-bases`를 적용하면 SQLAlchemy Column 객체에 대한 타입 대입 문제, Pydantic V2 `from_orm` 관련 경고, type annotation 누락 등 백엔드 소스 전반에서 약 118건의 타입 불일치 오류가 무더기로 검출되고 있습니다.
* **해결 방안**:
  - `pyproject.toml`에 `explicit_package_bases = true`를 추가해 namespace 충돌을 없애고, 검출되는 python 타입 불일치 오류들을 리팩토링하여 정적 검증 무결성을 달성해야 합니다.

### 8. Next.js 16 (Turbopack) Middleware Convention Deprecated 경고
* **현상 및 원인**:
  - 프론트엔드 production 빌드(`npm run build`) 실행 시 `⚠ The "middleware" file convention is deprecated. Please use "proxy" instead.` 경고 메시지가 콘솔에 출력됩니다. Next.js 최신 릴리즈의 Turbopack 사양에 어긋나는 구형 미들웨어 설정 방식이 잔존해 있습니다.
* **해결 방안**:
  - Next.js 공식 매뉴얼에 따라 기존 `middleware.ts`를 신규 프록시(`proxy`) 또는 라우팅 설정 구조로 전환하여 빌드 타임의 deprecation 경고를 소거합니다.

### 9. Pydantic V2 ORM 객체 마이그레이션 누락 경고 (`from_orm`)
* **현상 및 원인**:
  - 백엔드 `pytest` 테스트 수행 시, `src/api/services/auth_service.py` 등의 소스에서 Pydantic V2에서 deprecated 된 `.from_orm()` 메서드를 호출하여 `PydanticDeprecatedSince20` 경고가 지속 발생합니다.
* **해결 방안**:
  - Pydantic V2 표준에 발맞추어, 스키마 모델의 `model_config`에 `from_attributes = True` 설정을 지정하고, `.from_orm()` 호출 부분을 `model_validate()`로 일괄 변경합니다.
 
 ---
 
 ## 🚧 미완성 기능 및 확장 예정 작업 (To-Do) - 모두 구현 완료 (향후 고도화 항목 2건 추가 발굴)
 
 ### 1. SSR 환경 하이드레이션 (Hydration) 에러 원천 차단 (완료)
 * **구현 내용**:
   - 오디오 가동 엔진 및 WaveSurfer.js 컴포넌트는 Next.js의 `dynamic` import 구조를 적용하고 `{ ssr: false }` 옵션을 강제 적용하여 클라이언트 사이드에서만 마운트되도록 보장했습니다.
   - React의 `useEffect` 생명주기 훅 진입 시점에 브라우저 렌더링 상태임을 나타내는 플래그(`isMounted`)를 활성화한 후 오디오 인스턴스를 지연 초기화(Lazy Initialization) 완료했습니다.
 
 ### 2. 1초 주기 상태 폴링 (`use-project.ts`) 최적화 및 에러 핸들링 (완료)
 * **구현 내용**:
   - TanStack Query v5의 `refetchInterval`을 동적 콜백 함수로 전환했습니다.
   - 프로젝트의 `status` 필드가 `'pending'` 또는 `'processing'` 일 때만 `1000`(1초)을 반환하고, `'completed'`나 `'failed'`에 진입하면 즉시 `false`를 반환해 폴링을 즉시 중단(Short-circuiting) 시켰습니다.
   - 연속된 API 호출 실패 시 에러 임계치(3회 실패)를 설정하여 폴링을 일시 중지하는 에러 바운더리 핸들러를 내장했습니다.
 
 ### 3. GPU 메모리 OOM 방지 및 Celery 태스크 큐 병목 제어 (완료)
 * **구현 내용**:
   - Celery Worker의 동시성 설정(`celery_app.py` 내 `worker_concurrency=1` 적용)을 통해 음원 분리 태스크를 엄격하게 순차 처리(FIFO)하도록 제한했습니다.

### 4. 대기열 실시간 SSE(Server-Sent Events) 알림 채널 구축 (고도화 계획)
* **문제 배경**:
  - Celery 단일 컨커런시 FIFO 큐 제어로 인해, 여러 사용자가 동시 분석 요청 시 대기 대열의 상태 및 완료 대기시간 정보를 직관적으로 알 수 없는 문제가 있습니다.
* **구현 방향**:
  - 백엔드에 SSE 스트리밍 엔드포인트를 탑재하고, Celery Task의 대기열 순번과 리소스 가동 현황 데이터를 실시간으로 프론트엔드로 푸시하는 기능을 설계합니다.

### 5. WaveSurfer.js 재생부 동적 청킹 및 레이턴시 최소화 (고도화 계획)
* **문제 배경**:
  - 오디오 파일 용량이 매우 클 경우, Web Audio API 디코딩 및 파싱 시간 증가로 인해 UI 단락 현상이나 믹서 트랙 로딩 레이턴시가 발생할 우려가 있습니다.
* **구현 방향**:
  - 오디오 스트림을 재생 구간 위주로 동적 조각화(Dynamic Chunking)하여 디코딩 오버헤드를 낮추는 loading 기법을 적용합니다.

### 6. 다중 AI 음원 처리 모델 지원 및 사용자 선택 기능 (고도화 계획)
* **문제 배경**:
  - 현재는 오직 기본 `htdemucs` 모델만을 사용해 음원을 분리하고 있으나, 보컬 추출 감도나 특정 악기(드럼, 피아노) 분리 퀄리티 면에서 아쉬운 지점들이 존재합니다.
* **구현 방향**:
  - 음원 분리에 강점이 있는 `RoFormer`(vocal), `MDX-Net` 또는 `HTDemucs (6 stems)` 등의 다양한 AI 모델을 백엔드 Celery 파이프라인에 추가 도입합니다.
  - 사용자가 프로젝트 분석 요청 시, 원하는 AI 모델의 특성을 선택하여 분석할 수 있는 웹 UI 및 백엔드 라우터를 확장합니다.

### 7. 타브 악보 운지 최적화 알고리즘 정밀화 및 GuitarPro (.gp5/.gpx) 파일 내보내기 (고도화 계획)
* **문제 배경**:
  - Basic Pitch로 검출된 MIDI 정보 기반의 ASCII 타브 악보는 실제 기타리스트들이 운지하기에 비효율적인 프렛 배열을 추천하는 한계가 있습니다.
* **구현 방향**:
  - 손가락 이동 반경 최소화 및 난이도별 기타 지판 운지 최적화 알고리즘(Dynamic Programming 기반 운지 최적화)을 적용합니다.
  - 생성된 타브 데이터를 단순 화면 표출을 넘어, GuitarPro 호환 바이너리 파일(`.gp5`) 및 `MusicXML`로 변환해 다운로드할 수 있는 익스포트 파이프라인을 구축합니다.

### 8. Basic Pitch 퀀타이징 정밀도 향상 및 타브 운지법 플레이어빌리티(Playability) 알고리즘 보완 (완료 & 고도화 계획)
* **문제 배경**:
  - 기존 퀀타이징은 16분음표에 강제 snap되어 3연음(triplets) 등의 복잡한 리듬을 표현하지 못하고 찌그러지는 현상이 있었습니다.
  - 타브 악보 생성 시 개별 음들이 하나의 현에 중복 배치되어 음이 깨지거나, 프렛이 지나치게 도약하여 사람이 실제로 운지하기 불가능한 패턴을 유발했습니다.
* **구현 내용 및 향후 고도화 방향**:
  - **구현 완료**: 16분음표와 8분음표 셋잇단음표(Triplet) 그리드 간의 누적 오차를 비교하여 동적으로 스냅 그리드를 스위칭하는 퀀타이징 로직과 역할별(Melody/Bass/Harmony) Adaptive Noise Gate를 반영했습니다.
  - **구현 완료**: 직전 프렛 위치와의 차이에 따라 도약 페널티(Transition Penalty)를 가산하고, 동일 시간대의 중복 현 배치 방지(Occupied String Guard)를 통해 운지 정밀도를 높였습니다.
  - **구현 완료**: `music21`을 거쳐 생성되는 표준 악보(MusicXML)의 다성부 가독성을 보완하기 위해, 동일 온셋 시점에 위치한 개별 노트들을 `chord.Chord` 객체로 자동 병합하도록 리팩토링했습니다.
  - **향후 고도화**: 머신러닝 기반의 프렛 운지 예측 딥러닝 모델(예: TabCNN 계열 가중치 연동)을 경량화하여 백엔드 파이프라인에 결합하고, 운지 난이도별 최단 경로 Dynamic Programming 알고리즘을 추가 보완합니다.
 
 ---
 
 ## 📋 작업 추적 칸반 보드 (Kanban)
 
 ### ⬜ 진행 전 (To-Do List)
 - [ ] Backend: mypy `--explicit-package-bases` 활성화 및 백엔드 파이썬 타입스크립트/정적 타입 오류 118건 전면 해소
 - [ ] Frontend: Next.js 16 (Turbopack) `middleware` 컨벤션을 신규 `proxy` 사양으로 마이그레이션
 - [ ] Backend: Pydantic V2 `.from_orm()` 호출을 `.model_validate()` 및 `from_attributes=True` 사양으로 마이그레이션
 - [ ] Backend/Frontend: 대기열 실시간 SSE(Server-Sent Events) 알림 채널 구축
 - [ ] Frontend: WaveSurfer.js 재생부 동적 청킹 및 디코딩 레이턴시 최소화 로직 도입
 - [ ] Pipeline: 다중 AI 음원 처리 모델(RoFormer, MDX-Net 등) 선택 옵션 구현
 - [ ] Algorithm: 타브 악보 운지법 최적화 적용 및 GuitarPro (.gp5) 내보내기 모듈 개발
 - [ ] Algorithm: Basic Pitch 퀀타이징 정밀도 향상 및 타브 운지법 플레이어빌리티(Playability) 머신러닝/DP 최적화 알고리즘 추가 보완
 
 ### 🟦 진행 중 (In Progress)
 
 ### 🟩 완료됨 (Done)
 - [x] Frontend: Next.js `multitrack-player.tsx` 내 `dynamic(..., { ssr: false })` 래핑 보완 및 `isMounted` 렌더링 방어막 도입 (Ratchet Rule #402 준수)
 - [x] Frontend: Zustand 및 TanStack Query v5 `use-project.ts` 1초 폴링 최적화 (`refetchInterval` 단락 제어)
 - [x] Frontend: 프론트엔드 ESLint 및 Prettier 린트 에러 63건 완전 소거 (Perfect Clean Pass)
 - [x] Frontend: `npm run build` production 빌드 컴파일 무결 확인
 - [x] Backend: FastAPI 백엔드 리팩토링: `flake8` 위반 사항 수정 및 미사용 임포트 일괄 제거
 - [x] Backend: GPU 병목 대응을 위한 Celery 단일 컨커런시 큐(`worker_concurrency=1`) 구성 업데이트
 - [x] Algorithm: Basic Pitch 기반 16분음표/셋잇단음표 하이브리드 퀀타이징 및 역할별 Adaptive Noise Gate 구현 완료
 - [x] Algorithm: 타브 악보 생성 시 프렛 도약 페널티(Transition Penalty) 및 동시성 현 중복 충돌 차단 로직(Occupied String Checking) 적용 완료
 - [x] Algorithm: Score/MIDI 생성 시 동일 온셋 음표들의 music21 Chord 객체 자동 병합 및 가독성 최적화 완료
 - [x] DevOps: GitHub Actions Python 테스트 매트릭스 `pip` 캐시 경로 누락(`shell: bash` 지정) 문제 해결
 - [x] DevOps: GitHub Actions Node.js 20 Deprecated 경고 회피용 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` 환경변수 주입
 - [x] Backend: `src/api/routes/projects.py`와 `test_workflow.py` 간의 1차 API 테스트 스키마 호환 확인
 - [x] Documentation: `.agents/workflows/ratchet-rules.md` 및 `CHANGELOG.md` 최신화
 - [x] Verification: 백엔드 `pytest` 유닛/통합 테스트 50개 전원 패스 검증 완료
 - [x] Documentation: 프로젝트 전반 한글 및 영문 스펙 사양화 통합화 및 설계 구축 (`README_KR.md` 및 `작업판.md` 개편 완료)
 
 ---
 
 ## <부록: 영구 진화형 에러 래칫 규칙>
 
 개발 과정에서 반복해서 발생한 CI/CD 및 런타임 오류의 재발을 방지하기 위한 영구 강제 규칙 가이드라인입니다. 모든 서브에이전트와 기여자는 아래 규칙을 위반해서는 안 됩니다.
 
 ### 🛡️ Rule #402: Web Audio API / WaveSurfer 브라우저 샌드박스 원칙
 * **규칙 정의**: `AudioContext`, `WaveSurfer`, `window.document` 등을 호출하는 컴포넌트/인스턴스는 절대로 Next.js 서버 환경(Node.js SSR)에서 빌드 타임에 실행되거나 초기화되면 안 됩니다.
 * **강제 규칙**:
   - 모듈의 사용은 오직 클라이언트 렌더링 시점에 실행되도록 `typeof window !== 'undefined'` 조건절 분기를 태우거나, 리액트 마운트 완료 시점(`useEffect`) 내부로 초기화 코드를 감싸야 합니다.
   - 불필요한 서버 렌더링 예외 에러 방지를 위해 컴포넌트 임포트 시 `dynamic(() => import(...), { ssr: false })` 사용을 의무화합니다.
 
 ### 🛡️ Rule #403 & #404: CI/CD 파이프라인 캐시 및 의존성 방어 법칙
 * **규칙 정의**: CI/CD Runner(특히 Windows 빌드 환경)에서 환경 변수가 꼬이거나 캐시 경로 파싱 오류로 빌드가 폭발하는 현상을 미연에 차단합니다.
 * **강제 규칙**:
   - `actions/cache@v4`나 `actions/upload-artifact` 사용 시, 파일 경로나 캐시 경로가 널(Null) 값으로 할당되어 `Input required and not supplied: path` 에러가 나지 않도록 파이프라인 YAML을 정비합니다.
   - 서브쉘 출력을 파싱할 때는 OS별 기본 쉘 편차를 없애기 위해 반드시 `shell: bash` 지시어를 명시적으로 선언합니다.
   - Node.js 20 Deprecated 경고 로그로 빌드 에러가 마스킹되는 것을 방지하기 위해 최상단 환경변수에 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true`를 강제 설정합니다.
 
 ### 🛡️ Rule #405: API-테스트 코드 스키마 무결성 동기화 의무
 * **규칙 정의**: 백엔드 REST API의 응답 스키마(Pydantic, SQLAlchemy 모델 등)를 변경할 시에는, 해당 API를 호출하는 통합 테스트 코드(`tests/test_project_routes.py`, `test_workflow.py`)의 검증 로직도 반드시 동시에 패치하여 테스트 붕괴를 예방합니다.
 * **강제 규칙**:
   - 백엔드 라우터 및 스키마 파일 수정 직후에는 로컬 터미널에서 `pytest`와 `flake8`을 즉시 작동시켜 정합성을 점검합니다.
   - 어떠한 상황에서도 린트 에러(`flake8` Exit Code 1)가 발생한 상태로 master 브랜치에 코드를 푸시할 수 없습니다.

### 🛡️ Rule #406: Next.js 빌드 타임 조건부 컴파일러 Directive 예방 원칙
* **규칙 정의**: 특정 개발/테스트(Jest) 환경과 production build 환경 간의 Typescript 컴파일러 설정 편차로 인해, 특정 라인에 `@ts-expect-error`를 달아두었음에도 불구하고 빌드 시 "Unused '@ts-expect-error' directive"가 발생하여 빌드가 붕괴되는 현상을 예방합니다.
* **강제 규칙**:
  - 조건부로 에러가 나거나 나지 않는 타입 매핑 우회 시에는, 단독 지시어(`@ts-expect-error` 또는 `@ts-ignore`) 사용으로 빌드가 차단되는 것을 피하기 위해 `as any` 타입 캐스팅과 해당 라인의 ESLint 규칙 비활성화(`// eslint-disable-next-line @typescript-eslint/no-explicit-any`)를 동시에 안전히 조합하여 타입 무결성과 린트 빌드를 공존시켜야 합니다.

### 🛡️ Rule #407: Mypy Explicit Package Bases 강제화 및 정적 타입 무결성 원칙
* **규칙 정의**: 동일 모듈명을 공유하는 라우터와 내부 패키지의 네임스페이스 충돌을 방지하고, SQLAlchemy 및 Pydantic V2 마이그레이션 과정에서 발생하는 정적 타입 검증 오류로 인한 CI 빌드 중단을 예방합니다.
* **강제 규칙**:
  - `pyproject.toml`에 `explicit_package_bases = true`를 선언하고, 백엔드 정적 타입 체크 시 mypy 컴파일러 경고가 0건(Clean)이 되도록 보장해야 합니다.
  - SQLAlchemy ORM Column 타입 대입이나 Pydantic V2 schema 매핑 시, standard typing annotations을 준수하여 implicit type conversion 에러를 방지합니다.

### 🛡️ Rule #408: Next.js 16 Turbopack 하위 호환성 확보 및 Proxy 교체 규칙
* **규칙 정의**: Next.js 16.x 및 Turbopack 빌드 타임에서 더 이상 지원하지 않거나 Deprecated 경고를 유발하는 구형 설정 문법(예: 기존 `middleware.ts` 라우팅 규칙)을 방지합니다.
* **강제 규칙**:
  - Next.js build (`npm run build`) 실행 시 deprecation warning이 발견되는 경우, 즉시 최신 프록시(`proxy`) 문법이나 리액션 라우팅 규격으로 마이그레이션하여 컴파일러의 경고를 0으로 강제 유지합니다.
 
 ---
 
 ## 🤖 JustJam Architecture-Aligned Sub-Agents (서브 에이전트 팀)
 
 JustJam 프로젝트의 정밀하고 빠른 기능 수정 및 DevOps 안정성 유지를 위해 3개 페르소나의 서브 에이전트 협업 체계(Alpha, Beta, Gamma)를 수립하고 이를 작업판에 기록해 나침반으로 삼습니다.
 
 ### 1. 🎸 Agent Alpha: 프론트엔드 및 DSP 아키텍트 (Frontend & DSP Architect)
 - **주요 역할**: Next.js 16, React 19, Zustand, TanStack Query, WaveSurfer.js 등 프론트엔드 생태계 총괄.
 - **핵심 임무**:
   - `client/components/multitrack-player.tsx`의 오디오 스템 재생 및 메트로놈 동기화 제어.
   - `client/hooks/use-project.ts`의 백엔드 상태 폴링 훅 최적화 (status가 'completed' 또는 'failed'일 때 폴링 즉시 단락).
 - **절대 제약 (Ratchet Rule #402)**: 브라우저 전용 API(AudioContext, WaveSurfer 등)는 절대 서버(SSR)에서 렌더링되게 해서는 안 되며, 반드시 `typeof window !== 'undefined'` 분기나 `dynamic(() => import(...), { ssr: false })`를 적용해 하이드레이션(Hydration) 에러를 원천 차단.
 
 ### 2. 🧠 Agent Beta: 백엔드 및 AI 파이프라인 엔지니어 (Backend & AI Engine Engineer)
 - **주요 역할**: FastAPI 라우터 설계, Pydantic/SQLAlchemy 스키마 관리, Celery/Redis 기반 AI 모델 큐잉 최적화.
 - **핵심 임무**:
   - `src/api/routes/` 및 `src/api/services/`의 비즈니스 로직 작성 및 API 인터페이스(`client/lib/api.ts`) 간의 정합성 유지.
   - Demucs 및 Basic Pitch 구동 시 발생할 수 있는 GPU OOM(Out of Memory) 방지를 위해 Celery Worker의 동시성을 제한(`celery_app.py` 내 `worker_concurrency=1` 적용)하고 오디오 청킹을 제어.
 - **절대 제약 (Ratchet Rule #405)**: 백엔드 API 응답 스키마를 변경할 경우, 반드시 `test_workflow.py` 등 연관된 통합 테스트의 검증 로직(Assertion)도 동시에 업데이트하여 스키마 불일치로 인한 CI 붕괴를 제거.
 
 ### 3. 🛡️ Agent Gamma: DevOps 및 엄격 품질 검증관 (DevOps & Ratchet QA)
 - **주요 역할**: GitHub Actions CI/CD 파이프라인 수호, 파이썬 Lint(`flake8`, `black`) 강제, 래칫 규칙 위반 감시.
 - **핵심 임무**:
   - Alpha와 Beta가 작성한 코드를 감시하며, 사용하지 않는 임포트(F401), 줄바꿈 초과(E501) 등의 린트 에러를 즉각 찾아내어 `flake8 src/ tests/` 통과(Exit Code 0)를 강제.
   - `.github/workflows/` 내의 파이프라인 캐시 및 환경 변수 오류 방어.
 - **절대 제약 (Ratchet Rule #403, #404)**: GitHub Actions 워크플로우 수정 시 `actions/cache`의 `path`가 누락되지 않도록 검증하고, Node.js 20 Deprecated 환경 경고 회피용 환경 변수(`FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`)가 잘 적용되어 있는지 감시.
 
 ### ⚙️ 팀 운영 프로토콜 (Team Operating Protocols)
 1. **작업 전**: 무조건 프로젝트 루트의 `작업판.md`를 읽고 현재의 [To-Fix] 및 [To-Do] 상태를 파악.
 2. **작업 중**: Alpha와 Beta가 코드를 수정하면, Gamma가 로컬 샌드박스에서 즉시 `flake8`, `pytest`, `npm run build`를 실행하여 래칫 규칙 위반 여부를 검증.
 3. **작업 후**: 에러가 0건임이 확인되면 `작업판.md`의 칸반 보드 체크박스(`[ ]` -> `[x]`)를 스스로 갱신하고, 새로운 에러 해결 시 `<부록: 영구 진화형 에러 래칫 규칙>`에 새로운 룰을 영구 기록한 뒤 커밋.
